import { NextPage } from 'next';

import { Button } from 'components/UI';

const Home: NextPage = () => {
  return (
    <div>
      landing
      <Button className="fiva">
        fiva
      </Button>
    </div>
  );
};

export default Home;
